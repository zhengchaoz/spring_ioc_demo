server:
  port: 8080

# 配置模板工具类
spring:
  thymeleaf:
    prefix: classpath:/templates/   #设置页面前缀
    suffix: .html                   #设置页面后缀
    cache: false                    #是否使用缓存
  mvc:
    hiddenmethod:
      filter:
        enabled: true               #使用restFu需要开启此配置